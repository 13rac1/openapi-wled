type: object
description: >
  Configuration for transitions between states, including timing and palette changes.
  Controls how smoothly the device transitions between different states and effects.
properties:
  mode:
    type: boolean
    description: >
      Whether transitions are enabled. When true, changes in state will smoothly
      transition over the specified duration instead of changing instantly.
    example: false
  dur:
    type: integer
    minimum: 0
    maximum: 65535
    description: >
      Default transition duration in centiseconds (1/100th of a second).
      For example, a value of 7 means a transition takes 0.07 seconds.
      This affects all state changes unless overridden.
    example: 7
  pal:
    type: integer
    minimum: 0
    maximum: 255
    description: >
      Palette transition configuration. Controls how color palettes transition
      between states.
    example: 0
  rpc:
    type: integer
    minimum: 1
    maximum: 255
    description: >
      Random palette change interval in seconds. Amount of time between automatic
      random palette changes. Minimum is 1 second, maximum is 255 seconds.
    example: 60
example:
  mode: false
  dur: 7
  pal: 0
  rpc: 60 